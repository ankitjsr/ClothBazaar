@model List<ClothBazaar.Entity.Category>

@{
    ViewBag.Title = "Category List";
}

<h2>Category List</h2>

@if (!Model.Any())
{
    <p class="text-center">There are not Category!!</p>
}
else
{
    foreach (var item in Model)
    {
        <h3>@item.Name</h3>
    }
}

